int main()
{
    // Get input for row and columns
    // Make the map (2d array of R rows and C columns)

    for (int i = 0; i < R; i++)
        // scan in the character to index [i][j]
    // find the failure point(s)
    // figure out which failure point would cost more

}